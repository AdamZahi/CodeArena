server:
  port: 8080

spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: mistral
  datasource:
    url: jdbc:mysql://localhost:3306/codearena?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ""
  sql:
    init:
      mode: never
  mail:
    host: smtp.gmail.com
    port: 587
    username: codearenapi@gmail.com
    password: ecgq lgjy gvmh kxbs
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-70do2fpg2iw11ckn.us.auth0.com/
          jwk-set-uri: https://dev-70do2fpg2iw11ckn.us.auth0.com/.well-known/jwks.json




codearena:
  base-url: https://codearena.app

app:
  cors:
    allowed-origins: "http://localhost:4200" 
  shop:
    admin-email: islemazzouz04@gmail.com
    stripe:
      secret-key: dummy_test_secret_key
      publishable-key: dummy_test_publishable_key


stripe:
  secret-key: dummy_root_secret_key
  webhook-secret: dummy_root_webhook_secret

# Judge0 CE (Community Edition) — free, no API key required
judge0:
  base-url: https://ce.judge0.com
  api-key:
  timeout-seconds: 10

# Battle time limits per mode (in minutes, -1 = no limit)
battle:
  time-limit:
    duel-minutes: 30
    team-minutes: 40
    ranked-arena-minutes: 25
    blitz-minutes: 15
    practice-minutes: -1
    daily-minutes: -1
  ranker:
    base-url: ${ranker.base-url}
    connect-timeout-ms: 1500
    read-timeout-ms: 4000
    enabled: true
  classifier:
    base-url: ${classifier.base-url}
    connect-timeout-ms: 1500
    read-timeout-ms: 8000
    enabled: true



ai-model:
  url: ${ai-model}

piston:
  base-url: ${piston.base-url}
  run-timeout-ms: 15000
  memory-limit-bytes: 256000000

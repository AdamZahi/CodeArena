server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/codearena?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  mail:
    host: smtp.gmail.com
    port: 587
    username: islemazzouz04@gmail.com
    password: ycsm qcyo tqfk hlbd
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
    open-in-view: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-70do2fpg2iw11ckn.us.auth0.com/
          jwk-set-uri: https://dev-70do2fpg2iw11ckn.us.auth0.com/.well-known/jwks.json




codearena:
  base-url: https://codearena.app

app:
  cors:
    allowed-origins: "http://localhost:4200"
  shop:
    admin-email: islemazzouz04@gmail.com

# Piston (self-hosted code execution engine)
piston:
  base-url: http://192.168.0.195:2000
  run-timeout-ms: 15000
  memory-limit-bytes: 256000000

# Battle time limits per mode (in minutes, -1 = no limit)
battle:
  time-limit:
    duel-minutes: 30
    team-minutes: 40
    ranked-arena-minutes: 25
    blitz-minutes: 15
    practice-minutes: -1
    daily-minutes: -1
  # Score Ranker microservice (Python FastAPI wrapping ranker_model.pkl).
  # Falls back to a time-based estimate if unreachable, so battles never break.
  ranker:
    base-url: http://localhost:8010
    connect-timeout-ms: 1500
    read-timeout-ms: 4000
    enabled: true
  # Complexity Classifier microservice (Python FastAPI wrapping CodeBERT).
  # Predicts a Big-O class (O(1) … O(2^n)) for each submission. Submissions
  # keep resolving even when the classifier is offline — a fallback result
  # with score=0.0 is persisted instead.
  classifier:
    base-url: http://localhost:8011
    connect-timeout-ms: 1500
    read-timeout-ms: 8000
    enabled: true
